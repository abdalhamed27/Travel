// import jwt, { JwtPayload } from 'jsonwebtoken';

// const secret: string = process.env.NEXTAUTH_SECRET || 'your-secret-key';

// export function decodeToken(token: string): JwtPayload | null {

// }
